/*!
 * config.js
 * Copyright(c) 2013
 * Author: jifeng <jifeng.zjd@taobao.com>
 */

"use strict";

/**
 * Module dependencies.
 */

var fs = require('fs');

var config = {
  version: '2013092201',
  appname: 'webhook',
  port: 1723,
  git: 'git@gitlab.alibaba-inc.com:honeycomb/app-{appname}.git',
  rundir: __dirname + '/run/'
}

if (!fs.existsSync(config.rundir)) {
	fs.mkdirSync(config.rundir);
}

module.exports = config;

